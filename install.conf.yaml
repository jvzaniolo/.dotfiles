- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.sheldon:
      create: true
      path: sheldon

- create:
    - ~/Projects

- shell:
    - command: ./homebrew.zsh
      stdout: true
      stderr: true
